const https = require('http');  const data = JSON.stringify({      email: 'admin@aurasense.com',      password: 'Admin123!'  });  const options = {      hostname: 'localhost',      port: 3000,      path: '/api/auth/admin-login',      method: 'POST',      headers: {          'Content-Type': 'application/json',          'Content-Length': Buffer.byteLength(data)      }  };  const req = https.request(options, (res) =     let responseData = '';      res.on('data', (chunk) = responseData += chunk; });      res.on('end', () = console.log('Response:', responseData); });  });  req.on('error', (e) = console.error('Error:', e.message); });  req.write(data);  req.end(); 
